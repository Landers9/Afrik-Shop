<%= form_with(model: favory, local: true) do |form| %>
  <% if favory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(favory.errors.count, "error") %> prohibited this favory from being saved:</h2>

      <ul>
      <% favory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
